# Rate limit zone used by the Pong nginx site.
# Kept in conf.d so the zone is defined at http {} level without editing
# the distro's main nginx.conf.
limit_req_zone $binary_remote_addr zone=pong_connect:10m rate=5r/s;
